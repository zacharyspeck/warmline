# Copy to .env.local and fill in. NEVER put real keys here — .env.example is committed.

# --- Convex deploy (Production/Preview only; not needed for local dev) ---
# Create at https://dashboard.convex.dev/project/settings#production-deploy-keys
CONVEX_DEPLOY_KEY=

# --- Frontend (.env.local) ---
# Set automatically by `npm run dev`. Shown here for reference.
# NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# --- Server secrets ---
# Read by Convex ACTIONS, so they do NOT go in .env.local.
# Set them on the Convex deployment instead:
#   npx convex env set FIBER_API_KEY sk_live_xxxxxxxx
#   npx convex env set OPENAI_API_KEY sk-xxxxxxxx
# Listed here only so teammates know what to provision (example values, not real).
FIBER_API_KEY=sk_live_xxxxxxxxxxxxxxxx
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx

# Signup gate (Phase D2): the invite code checked server-side during signup.
# Set it on the Convex deployment; signups are REJECTED while it is unset
# (fail closed). Never commit the real value.
#   npx convex env set INVITE_CODE xxxx-xxxx-xxxx
INVITE_CODE=xxxx-xxxx-xxxx

# --- Connector OAuth (read by the Next route handlers in app/api/connectors) ---
# Create the OAuth apps yourself (secrets can't be provisioned for you) and add
# these to .env.local. Register redirect URI <origin>/api/connectors/<provider>/callback
# Google Cloud Console → APIs & Services → Credentials (enable People + Calendar APIs):
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
# Azure Portal → App registrations (Microsoft Graph: Contacts.Read, Mail.Read):
MICROSOFT_OAUTH_CLIENT_ID=
MICROSOFT_OAUTH_CLIENT_SECRET=
